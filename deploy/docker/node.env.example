# Example configuration for running the AGI Alpha Node container.
# Copy this file and adjust the values for your environment before passing it to
# `docker run --env-file` or mounting it at /config/node.env.

# Required configuration ----------------------------------------------------
NODE_LABEL=1
OPERATOR_ADDRESS=0x0000000000000000000000000000000000000001
RPC_URL=https://rpc.ankr.com/eth
ENS_PARENT_DOMAIN=alpha.node.agi.eth

# Networking -----------------------------------------------------------------
METRICS_PORT=9464
API_PORT=8080

# Optional Ethereum contract wiring -----------------------------------------
STAKE_MANAGER_ADDRESS=
PLATFORM_INCENTIVES_ADDRESS=
SYSTEM_PAUSE_ADDRESS=
DESIRED_MINIMUM_STAKE=
AUTO_RESUME=false

# Stake automation -----------------------------------------------------------
AUTO_STAKE=false
STAKE_AMOUNT=
INTERACTIVE_STAKE=true
OPERATOR_PRIVATE_KEY=

# Vault integration (optional) -----------------------------------------------
VAULT_ADDR=
VAULT_TOKEN=
VAULT_SECRET_PATH=
VAULT_SECRET_KEY=operatorPrivateKey

# Offline continuity ---------------------------------------------------------
OFFLINE_SNAPSHOT_PATH=
OFFLINE_MODE=false
LOCAL_MODEL_PATH=

# Advanced -------------------------------------------------------------------
PROJECTED_REWARDS=
CONFIG_PATH=
